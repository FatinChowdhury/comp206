#include <stdio.h>
int main(void)
{
        char c;
        puts("Freshman: ");
        c = getc(stdin);

        if (c == 'Y' || c == 'y')
                puts("Welcome\n");
        else
                puts("Try again.\n");
        return 0;
}
