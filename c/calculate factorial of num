#include <stdio.h>
int main(void) {
  int num, fact;
  fact = 1;
  printf("Enter your number: ");
  scanf("%d", &num); // assumes >= 0 and <= 12
  for (int i = 1; i <= num; i++) {
      fact = fact * i;
  }
  printf("The factorial of %d is %d/n", num, fact);
  return 0; // error code
}
